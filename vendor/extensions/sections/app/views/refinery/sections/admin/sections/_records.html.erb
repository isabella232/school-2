<%= render 'refinery/admin/search_header', :url => refinery.sections_admin_sections_path %>
<div class='pagination_container'>
  <% if @sections.any? %>
    <%= render 'sections' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
