<div class="container"> 
  <!--  breadcrumb -->
  <ul class="breadcrumb">
    <li><%= link_to root_page.title, refinery.root_path %></li>
    <li class="active"> <%= @page.menu_title %></li>
  </ul>
  <!-- Main Title-->
  <div class="titles">
    <div class="pull-left">
      <h1 class="headinglefttitle"><%= @page.menu_title %></h1>
      <div class="headingsubtitle">Список спортсменов и тренеров, занесенных на Доску почёта «Молодое Поколение – гордость Глазова» ДЮСШ № 1 за 2014-2015 учебный год. </div>
    </div>
    <i class="icon-folder-open-alt"></i> </div>
  <div class="row mt40">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
      <div class="container">
      <!-- Options-->
        <section id="options" class="clearfix">
          <ul id="filters" class="option-set clearfix" data-option-key="filter">
            <li><a href="#filter" data-option-value="*" class="selected">Показать все</a></li>
            <% @sections.each do |section| %>
              <li><a href="#filter" data-option-value=".section<%= section.id %>"><%= section.title %></a></li>
            <% end %>
          </ul>
        </section>
         <!-- Portfolio-->
        <ul id="portfoliocontainer" class="clickable clearfix row column5 best-students">
          <% @students.each do |student| %>      
            <li class='element <%= "section#{student.section.id}" %>' data-symbol="Hg" data-category="transition">
              <div class="thumbnail">
                <div class='img-thumbnail'>
                  <%= link_to (image_tag student.photo_url, alt: student.full_name, class: 'img-rounded'), refinery.students_student_path(student), remote: true  %>
                </div>
                <div class="caption"> 
                  <%= link_to student.full_name, refinery.students_student_path(student), remote: true, class: "portfoliotital" %> 
                  <strong><%= student.section.title %></strong> 
                </div>
              </div>
            </li>
          <% end %>  
        </ul>
      </div>
    </div>
  </div>
</div>
<div id='student-info'>
</div>  
