<% content_for :body do %>
  <ul id="students">
    <% @students.each do |student| %>
      <li>
        <%= link_to student.first_name, refinery.students_student_path(student) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
