<%= render 'refinery/admin/search_header', :url => refinery.students_admin_students_path %>
<div class='pagination_container'>
  <% if @students.any? %>
    <%= render 'students' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
