<%= render 'refinery/admin/search_header', :url => refinery.employees_admin_roles_path %>
<div class='pagination_container'>
  <% if @roles.any? %>
    <%= render 'roles' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
